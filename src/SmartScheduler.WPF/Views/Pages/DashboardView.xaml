<Page x:Class="SmartScheduler.WPF.Views.Pages.DashboardView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="1000"
      Background="{DynamicResource BackgroundBrush}"
      Title="DashboardView">

    <!--<ui:NavigationView
    PaneTitle="SmartScheduler"
    IsPaneOpen="True">

    -->
    <!-- Meniul lateral -->
    <!--
    <ui:NavigationView.MenuItems>
    <ui:NavigationViewItem Icon="{ui:SymbolRegular.Home24}" Content="Dashboard" />
    <ui:NavigationViewItem Icon="{ui:SymbolRegular.TaskList24}" Content="Tasks" />
    <ui:NavigationViewItem Icon="{ui:SymbolRegular.CalendarClock24}" Content="Free Time" />
    <ui:NavigationViewItem Icon="{ui:SymbolRegular.EmojiSparkles24}" Content="Hobbies" />
    <ui:NavigationViewItem Icon="{ui:SymbolRegular.Settings24}" Content="Settings" />
    <ui:NavigationViewItem Icon="{ui:SymbolRegular.Person24}" Content="Profile" />
    </ui:NavigationView.MenuItems>

    -->
    <!-- Conținutul paginii -->
    <!--
    <ui:NavigationView.Co>
    <Grid Margin="16" ColumnSpacing="16" RowSpacing="16">
    -->
    <!-- aici pui exact Grid-ul cu Calendar, Card-uri etc. -->
    <!--
    </Grid>
    </ui:NavigationView.Co>
    </ui:NavigationView>-->
</Page>